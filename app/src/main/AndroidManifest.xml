<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.criptabit">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/aplogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/aplogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Criptabit">
        <activity android:name=".ForgotPassword"></activity>
        <activity android:name=".Stockmarket" />
        <activity android:name=".StockChart" />
        <activity android:name=".Cryptomarket" />
        <activity android:name=".About" />
        <activity android:name=".CryptoChart" />
        <activity android:name=".UserProfile" />
        <activity android:name=".Signup" />
        <activity android:name=".Login" />
        <activity android:name=".Dashboard">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>